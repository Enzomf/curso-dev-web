// Array como sendo um conjunto unidimencional de dados
// dados armazenados em indices

var listaFrutas = Array();

function lista(frutas){
    var controle = document.getElementById('lista').value

}

// podem ser declarados das seguintes formas:

var primeiro = Array()
var segundo = []
var terceiro = new Array()